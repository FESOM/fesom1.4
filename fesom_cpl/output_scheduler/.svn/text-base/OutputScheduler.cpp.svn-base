#include "OutputScheduler.h"
#include <iomanip>
#include <sstream>
#include <cstdlib>

using namespace std;

namespace AWI
{
   OutputScheduler::OutputScheduler(std::string unit_, unsigned int first_, unsigned int rate_)
   : unit(unit_), first(first_), rate(rate_)
   {
   }
}